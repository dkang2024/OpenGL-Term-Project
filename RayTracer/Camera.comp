// Camera structure to make passing in important camera values cleaner, and initialize constants here
struct ViewerCamera{
    vec3 position;
    vec3 pixelDX; 
    vec3 pixelDY; 
    vec3 initPixelPos;
};